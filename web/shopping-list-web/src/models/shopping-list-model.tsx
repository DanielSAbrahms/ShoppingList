export type ShoppingList = {
    id: string;
    name: string;
    date: string;
    products: string[];
}

export type NewShoppingList = {
    name: string;
    date: string;
    products: string[];
}